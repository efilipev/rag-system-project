---
apiVersion: v1
kind: Service
metadata:
  name: llm-generation-service
  namespace: rag-system
  labels:
    app: llm-generation
spec:
  type: ClusterIP
  ports:
  - port: 8105
    targetPort: 8105
    protocol: TCP
    name: http
  selector:
    app: llm-generation
